import Index from "./pages/index/index";

function App() {
  return (
    <div
      className="App"
      style={{
        height: 200,
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <Index />
    </div>
  );
}

export default App;
